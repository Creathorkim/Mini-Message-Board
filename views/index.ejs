<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
  </head>
  <body class="bg-purple-100 p-4">
    <div>
      <h1 class="text-3xl font-bold my-4 text-center"><%= title %></h1>
      <a href="/new" class="text-blue-600 hover:underline mb-4 block"
        >Add new message</a
      >
      <div class="space-y-4">
        <% messages.forEach((msg)=>{ %>
        <div
          class="<%= currentUser === msg.user ? 'flex justify-start' : 'flex justify-end' %>"
        >
          <div class="mb-5">
            <div class="p-4 shadow rounded">
              <p class="text-lg font-semibold"><%= msg.user %></p>
              <p class="text-gray-700"><%= msg.text %></p>
            </div>
            <p class="text-sm text-gray-400">
              <%= msg.added.toLocaleString() %>
            </p>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
    <%- include("partials/footer") %>
  </body>
</html>
